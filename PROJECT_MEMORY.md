# 项目记忆

## 当前目标

将“北大附小智能阅读推荐”收敛为一个家长和小朋友可以直接打开的 GitHub Pages H5 链接：

https://lmissd.github.io/library_pku/

## 当前用户要求

- 不再展示 API 地址、Token、访问码、馆藏候选内容等技术配置。
- 网页开始时只展示两个入口：
  - 小朋友端
  - 家长端
- 小朋友端负责：
  - 姓名、年级、阅读兴趣、阅读历史
  - 找书推荐
  - 手动选择想读的书
  - “我已读完📖”
  - 阅读检测问题
- 家长端负责：
  - 阅读反馈
  - 孩子的阅读路径
  - 家长可以跟孩子说的话
  - 孩子的阅读画像
- 最小化使用门槛：用户只需要打开网页链接。

## 当前实现

- `index.html` 中已移除可见的“接口设置”区域。
- 首屏已拆成“小朋友端”和“家长端”两个入口，不再直接展示找书、问答、反馈三个窗口。
- 小朋友端展示找书表单、三层推荐书单、单本书详情和“我已读完📖”确认，再进入阅读小问答。
- 家长端只在孩子完成问答后展示评估结果、阅读路径、家长沟通建议和阅读画像；没有记录时提示先去小朋友端完成。
- 前端内部固定调用工作流地址，不让终端用户填写技术参数。
- 当前临时跑通版本默认使用内置本地书库，继续完成三层推荐、读完提问和阅读反馈，避免孩子使用时直接报错或等待后端鉴权失败。
- 页面代码中保留线上 Coze 工作流地址；后续 Coze 入口处理好鉴权后，可以再切回线上工作流优先。
- 内置本地书库当前包含漫画、校园、恐龙、科普、侦探、成长、经典、自然、神话等临时馆藏，并带虚拟馆藏位置。
- 本地书库推荐已改为主题相关优先：孩子输入明确主题后，不再为了凑满数量把明显无关主题的书混入结果；某一层没有匹配书时显示空层级提示。
- 页面错误提示改成普通用户能理解的话术，不再向孩子或家长展示 Token、API 等词。
- 使用 `localStorage` 保存页面进度，刷新后尽量不丢失。

## 重要技术边界

当前 Coze 工作流地址 `https://cqccmb7q97.coze.site/run` 匿名访问会返回 `401 Unauthorized`。

因此，当前 H5 通过本地书库兜底保证“点开可跑通”。长期正式版本如果要恢复完全由后端知识库/工作流生成，后端必须提供不暴露 Token 的公网入口：

- 让工作流支持免鉴权公网调用；或
- 增加一个安全代理服务，由代理保存 Token，前端只调用代理。

静态网页中不能安全隐藏 Bearer Token。

## 当前文件

- `index.html`：手机端 H5 主页面。
- `README.md`：项目说明。
- `PROJECT_MEMORY.md`：项目记忆。

## 2026-06-05 修复记录

- 修复孩子端点击“我回答完了”后本地评估报 `Cannot read properties of undefined (reading 'length')` 的问题。
- 根因是下一本推荐计算里把兴趣文本当成兴趣画像对象传入，导致本地书籍相关性打分读取 `activeGroups.length` 时报错。
- 已复测：动物/恐龙主题推荐《恐龙大陆》《神奇校车》《森林报》；选择《恐龙大陆》后完成 3 个问题提交，可进入“回答已保存”，家长端可显示阅读反馈、阅读路径和孩子阅读画像。
